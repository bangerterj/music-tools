# Studio DAW — Python backend dependencies
# Install with: pip install -r requirements.txt
# Note: torch must be installed first for GPU support:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
#   (or the CPU-only build if no GPU)

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
aiofiles>=23.0.0

# Audio I/O and manipulation
pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.2
numpy>=1.24.0

# Stem separation
demucs>=4.0.1

# Audio → MIDI
basic-pitch>=0.3.3

# Effects chain (supports VST plugins natively)
pedalboard>=0.9.0

# AI mastering
matchering>=2.0.6

# AI music generation (audiocraft = Meta's MusicGen)
# Install separately: pip install git+https://github.com/facebookresearch/audiocraft.git
# or: pip install audiocraft

# MIDI utilities
pretty_midi>=0.2.10
midiutil>=1.2.1

# MIDI → audio render (requires fluidsynth system binary)
pyfluidsynth>=1.3.3

# Pydantic for data models
pydantic>=2.0.0
